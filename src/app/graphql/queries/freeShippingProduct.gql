query FreeShippingProduct($handle: String!, $countryCode: CountryCode!)
@inContext(country: $countryCode) {
  product(handle: $handle) {
    priceRange {
      minVariantPrice {
        currencyCode
        amount
      }
    }
  }
}
